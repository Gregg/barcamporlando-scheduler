<h2>Editing Event <%= link_to @event.name, @event %></h2>

<%= error_messages_for :event %>

<% form_for(@event) do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>

  <p>
    <%= f.submit "Update" %>
    or
    <%= link_to 'Cancel', events_path %>
  </p>
<% end %>
